# 复制到 ECS：/opt/xszs-blog/deploy/.env
# cp deploy/.env.example /opt/xszs-blog/deploy/.env

# 由 CI 在 tag 部署时写入，也可手动指定回滚版本
IMAGE_TAG=latest

NUXT_PUBLIC_API_BASE=/api
NUXT_API_BASE=http://host.docker.internal:8080/api

# 可选
# NUXT_PUBLIC_LIVE2D_ENABLED=true
# NUXT_PUBLIC_CANVAS_NEST_ENABLED=true
