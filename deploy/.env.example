# 复制到 ECS：/opt/xszs-blog/deploy/
# cp deploy/docker-compose.yml deploy/release.sh /opt/xszs-blog/deploy/
# chmod +x /opt/xszs-blog/deploy/release.sh

# 由 release.sh 写入，或手动指定版本
IMAGE_TAG=v1.0.0

NUXT_PUBLIC_API_BASE=/api
NUXT_API_BASE=http://host.docker.internal:8080/api

# 可选
# NUXT_PUBLIC_LIVE2D_ENABLED=true
# NUXT_PUBLIC_CANVAS_NEST_ENABLED=true
